import { test } from "@playwright/test";

test.describe("playwright setup", () => {
  test.skip(true, "E2E browser tests belum diimplementasikan untuk repo ini.");

  test("placeholder", async () => {
    // Intentionally skipped.
  });
});
